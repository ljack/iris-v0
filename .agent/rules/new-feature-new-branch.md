---
trigger: always_on
---

When starting with stuff (feature/goal/etc), create new git branch and commit often to the branch.

Also push the branch to origin.

You can merge to master when tests pass, even in between development.
